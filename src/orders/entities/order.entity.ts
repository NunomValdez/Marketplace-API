export class Order {
    // product_id:       string;
    // user_id?:         string;
    // shipment_id:      string;
}

// maneira de pôr o nestjs a construir o schema no prisma, delegando assim essa tarefa para o nestjs em x de estar a fazer à pata 